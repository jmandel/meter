The meeting has ended. Please finalize minutes.md with the following:

1. Process any remaining transcript content above (if any).

2. Add a **## Summary** section at the end with:
   - A concise 3-5 sentence overview of the meeting: what was discussed, what was decided, and what remains open.
   - Highlight the most important decisions and any deadlines mentioned.

3. Review all inline `TODO(Name): ...` items across the document:
   - Keep them inline under the relevant topic sections.
   - Deduplicate them.
   - Ensure the assignee and any deadline mentioned are captured clearly.

4. Review the `## Attendees` section:
   - If you include it, make it one person per bullet.
   - Do not use a single comma-separated bullet of names.

5. Review tracker item references:
   - Normalize specific FHIR issues to canonical HL7 Jira links like `[FHIR-34735](https://jira.hl7.org/browse/FHIR-34735)`.
   - If the draft contains a noisy Jira URL for a specific issue, replace it with the canonical per-issue link.

6. Do a final pass on the full minutes for clarity and organization. Fix any rough edges from the incremental updates, but don't over-edit -- keep the structure that was built up during the meeting.
